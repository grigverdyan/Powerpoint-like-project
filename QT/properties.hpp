#ifndef PROPERTIES_HPP
#define PROPERTIES_HPP

enum MainWindowProperties
{
    windowSizeHorizontal = 1000,
    windowSizeVerticale = 600
};

enum SlideProperties
{
    slideSizeHorizontal = 600,
    slideSizeVerticale = 400
};

enum LeftSlidesButtonsProperties
{
    slideWidth = 130,
    slideHeight = 100
};

#endif // PROPERTIES_HPP
